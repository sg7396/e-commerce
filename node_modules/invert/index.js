
module.exports = function(str){
  var buf = '';
  var c;
  for (var i = 0, len = str.length; i < len; ++i) {
    c = str[i].toLowerCase(); 
    buf += c == str[i]
      ? c.toUpperCase()
      : c;
  }
  return buf;
};